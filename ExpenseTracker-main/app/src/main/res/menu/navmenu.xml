<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/dashboard"
        android:title="@string/dashboard"
        android:icon="@drawable/dashboard"
        app:showAsAction="ifRoom" /> <!-- Ensure this item appears if space allows -->

    <item
        android:id="@+id/income"
        android:title="@string/income"
        android:icon="@drawable/income"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/expense"
        android:icon="@drawable/expense"
        android:title="@string/expense"
        app:showAsAction="ifRoom" /> <!-- Use ifRoom to allow the item in ActionBar if space permits -->
</menu>
